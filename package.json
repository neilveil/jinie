{
  "scripts": {
    "start": "webpack server --config webpack.config.js",
    "pretty": "prettier --config .prettierrc --write .",
    "build": "rimraf build && NODE_ENV=production webpack --config webpack.config.js && rm build/*.LICENSE.txt && cp package.public.json build/package.json",
    "publish": "npm run build && cd build && npm publish"
  },
  "devDependencies": {
    "autoprefixer": "^10.2.6",
    "css-loader": "^5.2.6",
    "handlebars": "^4.7.7",
    "html-loader": "^2.1.2",
    "postcss-loader": "^5.3.0",
    "prettier": "^2.4.1",
    "rimraf": "^3.0.2",
    "sass": "^1.34.0",
    "sass-loader": "^11.1.1",
    "style-loader": "^2.0.0",
    "webpack": "^5.38.1",
    "webpack-cli": "^4.7.0",
    "webpack-dev-server": "^3.11.2"
  }
}
